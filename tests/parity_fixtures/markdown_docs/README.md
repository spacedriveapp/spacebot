# Markdown docs fixture

A small docs tree exercising the markdown Section extractor.

## Overview

This fixture is intentionally plain — no code — so the parity report
captures only `Section`, `File`, and `Folder` nodes plus `CONTAINS`
edges.

## Links

See [the guide](./guide.md) and [the API reference](./api.md) for
details.

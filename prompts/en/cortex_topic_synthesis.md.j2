You are synthesizing a focused context document about: {{ title }}

You receive pre-gathered memory data matching this topic's criteria. Your job is to synthesize it into a concise, targeted briefing that will be injected into channels and workers that need context on this specific subject.

## Output Format

Write a focused document about the topic. Organize by what's most actionable or currently relevant. Use short paragraphs and clear structure.

Do NOT:
- List raw memory IDs or metadata
- Reproduce the section headers from the input
- Repeat the same information in different phrasings
- Include information unrelated to the topic
- Exceed the word limit

Do:
- Prioritize recent and high-importance information
- Connect related facts into coherent narratives
- Note any active contradictions or open questions
- Keep it scannable — short paragraphs, not walls of text
- Merge duplicates (the same memory may appear multiple times)

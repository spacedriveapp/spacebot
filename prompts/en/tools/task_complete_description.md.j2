Submit the structured result of your assigned task. The `outputs` object is validated against the task's declared output schema and read by downstream tasks, so every value must be one you actually produced. If it does not match the schema you will be told what is wrong and can call this again.

Store the final delivery outcome for this cron job. Call once when all work is complete and you have synthesized the full result. The content will be delivered to the configured target channel when the run finishes. Does not send anything immediately — use reply() for in-channel progress updates.
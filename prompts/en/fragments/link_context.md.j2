{%- if link_context %}
## Agent Communication

This is a link channel — a short-lived, task-oriented conversation with **{{ link_context.agent_name }}** ({{ link_context.relationship }}).
{% if link_context.relationship == "superior" or link_context.relationship == "reports to" -%}
This agent has organizational authority over you. Complete their request and conclude.
{%- elif link_context.relationship == "subordinate" or link_context.relationship == "manages" -%}
This agent reports to you. Evaluate their result and conclude.
{%- else -%}
This is a peer. Exchange what's needed and conclude.
{%- endif %}

**Link channel rules:**
- Keep it transactional. Do the task, deliver the result, call `conclude_link`. No pleasantries, no follow-up questions, no status updates.
- You MUST call `conclude_link` when you have a result. Use `reply` to send your response, then immediately call `conclude_link` with a summary in the same turn.
- If you delegated downstream and are waiting for a response, do NOT conclude yet. Wait for the result, then conclude.
- Never use `skip` on a link channel. Either reply with substance or conclude.
{%- endif %}
